<UserControl x:Class="MyToDo.Views.IndexView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyToDo.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="15,10" FontSize="22" Text="你好Hello,今天是 2026年3月8日"/>

        <ItemsControl Grid.Row="1" Margin="0,20,0,0" ItemsSource="{Binding TaskBars}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="4"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="10" Background="{Binding Color}" CornerRadius="5">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect Color="#DDDDDD"
                                                                  ShadowDepth="1"
                                                                  BlurRadius="10">
                                                    
                                                </DropShadowEffect>
                                            </Setter.Value>
                                        </Setter>
                                        
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <StackPanel Margin="20,10">
                                <materialDesign:PackIcon Kind="{Binding Icon}" Width="30" Height="30"  Margin="15,0"/>
                                <TextBlock Margin="0,15" FontSize="15" Text="{Binding Title}"/>
                                <TextBlock Text="{Binding Content}" FontSize="40" FontWeight="Bold"/>
                            </StackPanel>
                            <Canvas ClipToBounds="True">
                                <Border CornerRadius="100" Canvas.Top="10" Canvas.Right="-50" Width="120" Height="120" Background="#FFFFFF" Opacity="0.1"/>
                                <Border CornerRadius="100" Canvas.Top="80" Canvas.Right="-30" Width="120" Height="120" Background="#FFFFFF" Opacity="0.1"/>
                            </Canvas>


                        </Grid>

                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>

        </ItemsControl>

        <Grid Grid.Row="2" Margin="0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Margin="10,0" CornerRadius="5" Background="#BEBEBE" Opacity="0.1"/>

            <DockPanel Margin="10,0">
                <DockPanel Margin="10,5" DockPanel.Dock="Top" LastChildFill="False">
                    <TextBlock Text="待办事项" FontSize="20" FontWeight="Bold"></TextBlock>
                    <Button Width="30" Height="30"   DockPanel.Dock="Right" VerticalAlignment="Top" Style="{StaticResource MaterialDesignFloatingActionButton}" >
                        <materialDesign:PackIcon Kind="Add"></materialDesign:PackIcon>
                    </Button>
                </DockPanel>
                <ListBox HorizontalContentAlignment="Stretch" ScrollViewer.VerticalScrollBarVisibility="Hidden"    ItemsSource="{Binding ToDoDtos}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <DockPanel Margin="10" LastChildFill="False">
                                <ToggleButton DockPanel.Dock="Right"/>
                                <StackPanel >
                                <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Content}" Margin="0,5"/>
                            </StackPanel>
                            </DockPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
    

            <Border Grid.Column="1" Margin="10,0" CornerRadius="5" Background="#BEBEBE" Opacity="0.1"></Border>
            <DockPanel Grid.Column="1"  Margin="10,0">
                <DockPanel Margin="10,5" DockPanel.Dock="Top" LastChildFill="False">
                    <TextBlock Text="备忘录" FontSize="20" FontWeight="Bold"></TextBlock>
                    <Button Width="30" Height="30"   DockPanel.Dock="Right" VerticalAlignment="Top" Style="{StaticResource MaterialDesignFloatingActionButton}" >

                        <materialDesign:PackIcon Kind="Add"></materialDesign:PackIcon>
                    </Button>
                </DockPanel>
                <ListBox ItemsSource="{Binding MemoDtos}">
                    <ListBox.ItemTemplate>
                        <DataTemplate> 
                                <StackPanel Margin="10">
                                    <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Content}" Margin="0,5"/>
                                </StackPanel> 
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox> 
            </DockPanel>
        </Grid>
    </Grid>
</UserControl>
